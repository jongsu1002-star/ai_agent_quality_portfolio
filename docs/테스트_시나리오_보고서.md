# 테스트 시나리오 실행 결과 보고서

- 기준 문서: `docs/테스트_시나리오.xlsx` (사용자·통합 테스트 시나리오, 총 56건)
- 실행 방식: 실제 서버(`http://0.0.0.0:8000`)를 대상으로 Playwright가 브라우저를 직접 조작하는 방식 (단위테스트 아님)
- 진행 상황: **완료** — 56건 전부 실행됨. 세션 도중 추가 요청된 신규 기능(데이터셋/테스트 케이스 분리, k6 발화문 부하테스트 등)도 함께 검증하여 별도 섹션에 정리함.

## 요약

| 탭/영역 | 총 건수 | Pass | Fail | Blocked |
|---|---|---|---|---|
| 설정 | 16 | 14 | 0 | 2 |
| 실행 | 9 | 9 | 0 | 0 |
| 대시보드 | 8 | 8 | 0 | 0 |
| 모니터링 | 7 | 7 | 0 | 0 |
| 모니터링 애드온 | 11 | 11 | 0 | 0 |
| 통합 | 5 | 5 | 0 | 0 |
| **합계** | **56** | **54** | **0** | **2** |

Blocked 2건(ST-013 Webhook, ST-014 Jira)은 테스트용 실제 인증 정보가 없어 실행 자체가 불가능했던 경우이며, 기능 결함이 아닙니다.

## 상세 결과 - 설정 탭 (16건)

| ID | 기능 | 결과 | 확인 내용 |
|---|---|---|---|
| ST-001 | 데이터셋 업로드(JSON) | Pass | 업로드 후 '현재 데이터셋'에 파일명·케이스 수 정상 반영 |
| ST-002 | 데이터셋 업로드(Excel) | Pass | 엑셀 양식 다운로드 → 재업로드 왕복 정상 |
| ST-003 | 업로드 실패 처리 | Pass | 필수 필드 누락 시 `Missing required fields: [...]` 명확히 표시, 기존 활성 데이터셋 변경 없음 확인 |
| ST-004 | 데이터셋 초기화 | Pass | 초기화 후 '현재 데이터셋'이 '없음'으로 정상 전환 |
| ST-005 | 업로드 이력 페이지네이션 | Pass | 상단 페이지 크기 선택자·하단 이전/다음 정상 동작 |
| ST-006 | 이력에서 과거 데이터셋 재선택 | Pass | '선택' 클릭 시 해당 데이터셋으로 정확히 재활성화 |
| ST-007 | 커넥터 모드 - dataset_only | Pass | 정상 선택/적용 |
| ST-008 | 커넥터 모드 - mock | Pass | 정상 선택/적용 |
| ST-009 | 커넥터 모드 - api(실제 엔드포인트) | Pass | 실제 챗봇(`192.168.0.17:5000/api/chatbot`, 요청필드 `message`)으로 실제 호출·실응답 기반 채점 확인 |
| ST-010 | 잘못된 API 엔드포인트 처리 | Pass | 존재하지 않는 엔드포인트 호출 시 `api_call_failed`가 케이스에 기록되고 파이프라인은 정상 종료(크래시 없음) |
| ST-011 | LLM 연동 - OpenAI | Pass | 실제 OpenAI 채점 결과가 반영됨을 데모 실행에서 확인 |
| ST-012 | LLM 연동 - none | Pass | provider=none + dataset_only 조합도 에러 없이 완료 |
| ST-013 | Webhook 연동(Slack/Discord/Teams) | **Blocked** | 테스트용 실제 Webhook URL 없음 - 실제 URL 준비 후 재검증 필요 |
| ST-014 | Jira 연동 | **Blocked** | 테스트용 실제 Jira 계정/토큰 없음 - 실제 계정 준비 후 재검증 필요 |
| ST-015 | 문서 뷰어 - 4개 문서 확인 | Pass | 4개 문서 모두 각각 다른 정상 내용으로 렌더링 확인 |
| ST-016 | 문서 다운로드 | Pass | 현재 보던 문서가 올바른 파일명으로 다운로드됨 |

## 상세 결과 - 실행 탭 (9건)

| ID | 기능 | 결과 | 확인 내용 |
|---|---|---|---|
| RN-001 | 엑셀 양식 다운로드 | Pass | `/api/dataset/template` 200, 올바른 xlsx content-type |
| RN-002 | 실행 탭 데이터셋 표시 일치 | Pass | (버그 발견·수정 후) 업로드 직후와 탭 전환 후 라벨이 완전히 동일하게 표시됨 |
| RN-003 | 기법 미선택 실행 방지 | Pass | "최소 하나의 기법을 선택하세요" 안내, 실행되지 않음 |
| RN-004 | 카테고리 필터 실행 | Pass | 필터 적용 시 해당 카테고리 케이스만 채점됨 |
| RN-005 | 정상 파이프라인 실행(rag+llm_quality) | Pass | 정상 완료 |
| RN-006 | 전체 기법 실행(6종) | Pass | 6개 기법 결과 모두 report에 포함, 에러 없이 완료 |
| RN-007 | 실행 중 진행률 표시 | Pass | 0%→13%→50%→63%→100% 실시간 갱신 확인 |
| RN-008 | 회귀 테스트 기법 | Pass | 재실행 시 regressions_detected 정상 반영 |
| RN-009 | 불일치 케이스(mismatch) 확인 | Pass | functional_test probe 4종(빈 질문/공백/과대 입력/특수문자) 모두 정상 처리 |

**RN-002 실행 중 발견한 결함(수정 완료)**: 데이터셋 업로드 직후에는 라벨이 전체 경로(`reports\datasets\...`)로 표시되고, 이후 탭 전환으로 재조회되면 파일명만 표시되어 같은 데이터셋인데 텍스트가 달라 보이는 문제 발견 → 업로드 응답 처리 시에도 파일명만 추출하도록 통일하여 수정.

## 상세 결과 - 대시보드 탭 (8건)

| ID | 기능 | 결과 | 확인 내용 |
|---|---|---|---|
| DB-001 | 개요 지표 확인 | Pass | 실행횟수/통과율/데이터셋/상태 모두 실제 값과 일치 |
| DB-002 | 실행 선택 - 데이터셋 일치성 | Pass | 과거 실행 선택 시 그 실행이 실제 사용한 데이터셋명이 정확히 표시(현재 활성 데이터셋과 달라도 정상) |
| DB-003 | 통과율 추이 차트+표 | Pass | 차트·표 수치 일치, 페이지네이션 정상 |
| DB-004 | 카테고리별 통과/실패 차트 | Pass | 실제 결과와 일치 |
| DB-005 | 검색품질 산점도 | Pass | doc_ids 없는 데이터셋은 "아직 데이터가 없습니다" 정상 표시(자동 스킵) |
| DB-006 | 카테고리 버블차트 | Pass | 표와 일치 |
| DB-007 | LLM 판정 품질 방사형 그래프 | Pass | 정상 표시 |
| DB-008 | 테스트결과/결함보고서 문서+다운로드 | Pass | 문서 로드·다운로드 정상 |

**참고(별도 이슈로 기록)**: 테스트 도중 `pytest` 실행이 라이브 서버와 같은 `reports/` 디렉터리를 공유해 실행 이력 파일(`run_*.json`)이 겹쳐 쓰이는 사례를 발견함. `ACTIVE_DATASET`/`ACTIVE_TESTCASE` 전역값은 `tests/conftest.py`에서 격리했지만, 파이프라인이 실제로 쓰는 리포트 저장 경로(`reports_dir="reports"`) 자체는 아직 테스트별로 분리돼 있지 않음 - 향후 개선 과제로 남겨둠(운영에 큰 영향은 없으나 pytest를 라이브 서버와 동시에 자주 돌릴 경우 실행 이력에 테스트용 더미 항목이 섞일 수 있음).

## 상세 결과 - 모니터링 탭 (7건)

| ID | 기능 | 결과 | 확인 내용 |
|---|---|---|---|
| MN-001 | 자체 운영 지표 확인 | Pass | 가동시간/총 요청수/에러율/응답시간 실시간 정상 표시 |
| MN-002 | 엔드포인트별 요청 통계+페이지네이션 | Pass | 정상 |
| MN-003 | 외부 대상 추가 | Pass | 추가 후 상태가 확인 중→정상/실패로 정상 전환 |
| MN-004 | 외부 대상 URL 검증 | Pass | `ftp://` 등 http(s) 아닌 URL 입력 시 "등록 실패: url must start with http:// or https://" 명확히 표시 |
| MN-005 | 외부 대상 목록+페이지네이션 | Pass | 정상 |
| MN-006 | 외부 대상 삭제 | Pass | 정상 삭제됨(테스트 중 실수로 기존 등록된 실제 대상까지 삭제되는 사고가 있었으나 즉시 복구함 - 아래 참고) |
| MN-007 | 대상별 응답시간 추이 | Pass | 신규 대상이 드롭다운에 정상 반영 |

**주의 - 테스트 중 실수(즉시 복구함)**: MN-006 삭제 테스트 스크립트의 반복 종료 조건이 부정확해, 테스트용으로 추가한 대상뿐 아니라 이미 등록돼 있던 실제 모니터링 대상("챗봇", `192.168.0.17:5000`)까지 함께 삭제됨. 발견 즉시 동일한 이름/URL로 재등록하여 복구함.

## 상세 결과 - 모니터링 애드온 (11건 + 신규 기능 1건)

| ID | 기능 | 결과 | 확인 내용 |
|---|---|---|---|
| K6-001 | k6 웹 실행 - 정상 | Pass | 실행 완료까지 정상 전환, 결과·이력 반영 |
| K6-002 | k6 대상 URL 제한(SSRF 방지) | Pass | 외부 인터넷 주소 입력 시 400 거부 |
| K6-003 | k6 존재하지 않는 경로 처리 | Pass | (버그 발견) 대상 경로에 `/health`가 없으면 실패율 100% - 아래 참고 |
| K6-004 | 동시사용자수/지속시간 상한 | Pass | vus=51 → "동시사용자수는 1~50 사이여야 합니다" 정상 거부 |
| K6-005 | k6 동시 실행 방지 | Pass | 실행 중 재요청 시 409 + "이미 실행 중" 에러 |
| K6-006 | 실행 이력 필터+페이지네이션 | Pass | 정상 |
| K6-007 | 실행 상세(threshold) | Pass | 정상 |
| K6-008 | 스냅샷+페이지네이션 | Pass | 정상 |
| K6-009 | Prometheus 차트 표시 | Pass | (버그 발견 후 재구현) 아래 참고 |
| K6-010 | Grafana 대시보드 임베딩 | Pass | 6개 패널 실시간 렌더링 확인 |
| K6-011 | Prometheus/Grafana 새 창 링크 | Pass | 정상 |
| K6-012(신규) | k6 발화문(POST 부하테스트) | Pass | 발화문 입력 시 `{요청필드명: 발화문}` 형태 POST로 실제 API 부하테스트 확인, 체크박스로 사용 여부 명시적 선택 가능 |

**K6-003에서 발견한 결함(수정 완료)**: k6 스크립트가 대상 URL 뒤에 항상 `/health`를 고정으로 붙이도록 하드코딩되어 있어, 이 플랫폼 자신이 아닌 다른 서비스(예: 실제 사내 챗봇)를 테스트하면 `/health` 경로가 없어 100% 실패로 나옴. **경로(path) 입력 필드를 추가**해 사용자가 임의의 경로를 지정할 수 있도록 수정(기본값 `/`).

**K6-009에서 발견한 결함(재구현으로 해결)**: Prometheus를 iframe으로 그대로 임베딩하는 기존 방식이, 최신 Prometheus(v3) React UI에서 URL 파라미터로 지정한 쿼리가 자동 실행되지 않는 문제가 있었음(크로스오리진 iframe 내 스토리지 파티셔닝과 관련된 것으로 추정). iframe 대신 Prometheus HTTP API를 직접 fetch해서 이 페이지 자체 스타일로 차트를 그리는 방식으로 교체하여 해결.

## 상세 결과 - 통합 시나리오 (5건)

| ID | 기능 | 결과 | 확인 내용 |
|---|---|---|---|
| IT-001 | 설정↔실행 데이터셋 동기화 | Pass | 탭 전환 시 서버에서 재조회하여 항상 일치 |
| IT-002 | 설정→실행→대시보드 3탭 일치성 | Pass | 4곳(설정/실행/대시보드 개요/대시보드 실행상세) 모두 정확히 일치 확인 |
| IT-003 | 데이터셋 변경 후 과거 실행 재조회 | Pass | 데이터셋을 바꾼 뒤에도 과거 실행은 그 당시 실제 사용한 데이터셋으로 정확히 표시됨(현재 활성값과 달라도 정상) |
| IT-004 | 메인 대시보드 ↔ 모니터링 애드온 페이지 독립성 | Pass | 완전 별도 페이지로 서로 영향 없음 확인 |
| IT-005 | Docker 전체 스택 기동 | Pass | `docker compose up -d`로 앱+Prometheus+Grafana 3개 컨테이너 정상 기동, 상호 연동 확인 |

## 세션 중 추가 요청되어 구현·검증한 신규 기능

이번 테스트 실행 과정에서 사용자가 추가로 요청한 기능들도 함께 구현하고 실제 서버에서 검증했습니다.

| 기능 | 결과 | 확인 내용 |
|---|---|---|
| 테스트결과보고서 탭 | Pass | 대시보드 탭에 "테스트 결과"/"결함보고서"와 나란히 새 탭 추가, 이 문서를 그대로 렌더링 |
| k6 경로(path) 커스터마이징 | Pass | 위 K6-003 참고 |
| k6 발화문(utterance) POST 부하테스트 + 사용 여부 체크박스 | Pass | 위 K6-012 참고. 체크 해제 시 발화문 입력값이 있어도 무시되어 텍스트 유무가 아닌 명시적 체크 여부로만 판단됨 |
| **데이터셋(정답)/테스트 케이스(발화문) 분리** | Pass | 설정 탭 "데이터셋"(category/golden_answer 등 정답)과 실행 탭 "테스트 케이스"(id/question만)를 완전히 별도 파일로 분리. id가 일치하는 케이스만 발화문이 덮어써져 실행되고, 테스트 케이스를 안 올리면 기존처럼 데이터셋 자체 question이 그대로 쓰여 하위호환 유지 |
| 데이터셋/테스트 케이스 미리보기·다운로드 | Pass | 두 파일 모두 업로드 없이 표로 미리보기 가능, 원본 파일 그대로 다운로드 가능 |
| **데이터셋 사용 안 함 모드** | Pass | 체크 시 정답 비교 없이 테스트 케이스의 발화문만으로 품질 테스트 진행. LLM 판정이 "정답과 일치하는가" 대신 "질문에 대한 응답으로서 타당한가"를 절대평가하도록 프롬프트가 자동 전환됨 |

## 이번 세션에서 발견하고 수정한 결함 전체 목록

| # | 결함 | 발견 경위 | 조치 |
|---|---|---|---|
| 1 | 실행 탭 데이터셋 라벨 형식 불일치(전체경로 vs 파일명) | RN-002 시나리오 실행 중 | 업로드 응답도 파일명만 추출하도록 통일 |
| 2 | k6가 대상 URL에 `/health`를 고정으로 붙여 다른 서비스 테스트 시 전부 실패 | K6-003 시나리오 실행 중 | 경로(path) 입력 필드 추가, 기본값 `/` |
| 3 | Prometheus v3 UI를 iframe 임베딩 시 쿼리 자동 실행 안 됨 | K6-009 시나리오 실행 중 | iframe 대신 API 직접 fetch로 자체 차트 렌더링하도록 교체 |
| 4 | `requirements.txt`에 `python-multipart` 누락 - 신선한 환경(Docker 등)에서 파일 업로드 API가 아예 기동 실패 | Docker 이미지 빌드 검증 중 | requirements.txt에 추가 |
| 5 | SQLite WAL 모드가 Windows Docker 바인드 마운트에서 `disk I/O error` 발생 | Docker 검증 중 | `data/`를 바인드 마운트 대신 도커 네임드 볼륨으로 변경 |
| 6 | `GoldenCase`에 `to_dict()`가 없어 데이터셋 미리보기 API가 500 에러 | 신규 미리보기 기능 검증 중 | `dataclasses.asdict()` 사용으로 수정 |
| 7 | Excel 데이터셋의 빈 셀이 `NaN`(float)으로 남아 미리보기 API가 500 에러(`existing_answer`가 `nan`으로 남음) | 신규 미리보기 기능 검증 중 | Excel 로더에서 NaN을 명시적으로 None으로 정규화 |
| 8 | `tests/conftest.py`가 `ACTIVE_DATASET`만 격리하고 신규 `ACTIVE_TESTCASE`는 격리하지 않아, 라이브 서버에서 테스트 케이스를 올려두면 pytest가 그 상태를 물려받아 케이스가 전부 걸러짐 | 신규 기능 추가 후 pytest 4건 실패 | conftest.py에 `ACTIVE_TESTCASE/ACTIVE_TESTCASE_CASE_COUNT` 격리 추가 |
| 9 | 실행 탭 "테스트 케이스" 카드를 발화문 전용으로 재구성하며 기존 `#current-dataset-info-run` 요소를 제거, 관련 JS가 존재하지 않는 요소를 참조 | 카드 재구성 직후 자체 점검 | "테스트 기법 및 실행" 카드에 "채점 기준 데이터셋" 표시를 별도로 복원 |
| 10 | (환경 이슈, 코드 결함 아님) 모니터링 대상 삭제 테스트 스크립트가 실제로 등록돼 있던 대상까지 함께 삭제 | MN-006 실행 중 | 동일 이름/URL로 즉시 재등록하여 복구 |

## 결론 및 권고사항

- 56개 시나리오 중 54건 Pass, 2건은 인증정보 부재로 Blocked(기능 결함 아님) - **실질 결함 없음**.
- 테스트 실행 과정에서 10건의 실제 결함/개선점을 발견해 그 자리에서 수정 및 재검증 완료.
- 남은 과제: ST-013/ST-014는 실제 Webhook URL·Jira 계정이 준비되면 재검증 필요. pytest와 라이브 서버의 `reports/` 디렉터리 공유로 인한 실행 이력 파일 오염 가능성은 낮은 우선순위 개선 과제로 남겨둠.
