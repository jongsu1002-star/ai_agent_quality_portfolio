.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
data/*.db
data/*.db-wal
data/*.db-shm
desktop_app/build/
desktop_app/*.spec

# 앱 실행 중 자동 생성되는 산출물(업로드 이력, 실행 결과, 설정 등) - 소스 아님
reports/

# scripts/backup_data.py 산출물 - 계정/세션 DB 스냅샷이라 커밋하면 안 됨(민감정보 포함 가능)
backups/
