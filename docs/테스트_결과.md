# 테스트 결과

> 이 문서는 `conftest.py`의 `pytest_terminal_summary` 훅이 `pytest` 실행 시마다 자동으로 재생성합니다. 수동으로 편집하지 마세요.
> 테스트를 일부만 선택해 실행한 경우, 이 문서에는 그 실행 범위만 반영됩니다.

- 최종 실행 시각: 2026-07-20T10:27:58
- 종료 코드: 0 (성공)
- 총 테스트 수: 472
- 통과: 472
- 실패: 0
- 건너뜀: 0
- 오류: 0

## 파일별 결과

| 테스트 파일 | 설명 | 건수 | 결과 |
|---|---|---|---|
| `tests/test_api_features.py` | - | 1 | 통과 |
| `tests/test_api_features.py` | JSON 데이터셋 업로드 API가 케이스 수를 올바르게 반환하는지 확인 | 1 | 통과 |
| `tests/test_api_features.py` | 데이터셋 디렉터리 밖 경로를 선택하려 하면 거부되는지 확인(경로 조작 방지) | 1 | 통과 |
| `tests/test_api_features.py` | 데이터셋 업로드 이력 목록이 최신순 정렬과 활성 표시를 올바르게 보여주는지 확인 | 1 | 통과 |
| `tests/test_api_features.py` | 데이터셋 초기화 시 활성 데이터셋이 비워지고 기본값으로 돌아가는지 확인 | 1 | 통과 |
| `tests/test_api_features.py` | 실행 상태 조회/이력 조회 API가 정상 동작하는지 확인 | 1 | 통과 |
| `tests/test_api_features.py` | 엑셀 양식을 내려받아 그대로 업로드한 뒤 파이프라인을 실행해도 정상 동작하는지 확인 | 1 | 통과 |
| `tests/test_api_features.py` | 이력에서 이전 데이터셋을 선택하면 그 데이터셋이 다시 활성화되는지 확인 | 1 | 통과 |
| `tests/test_auth.py` | - | 19 | 통과 |
| `tests/test_auth_hash.py` | - | 4 | 통과 |
| `tests/test_board.py` | - | 16 | 통과 |
| `tests/test_board_api.py` | - | 11 | 통과 |
| `tests/test_docs_generation.py` | - | 3 | 통과 |
| `tests/test_docs_generation.py` | 결함보고서.md가 실패 케이스와 회귀/기능시험 결과를 올바르게 담아내는지 확인 | 1 | 통과 |
| `tests/test_docs_generation.py` | 테스트 결과 마크다운이 총계/통과/실패 건수를 올바르게 요약하는지 확인 | 1 | 통과 |
| `tests/test_docs_generation.py` | 테스트_결과.md 파일이 실제로 생성되고 전체 통과 문구가 들어가는지 확인 | 1 | 통과 |
| `tests/test_docs_reference_integrity.py` | 문서의 표 안 코드span에 이스케이프 안 된 파이프(\|)가 있어 표가 깨지지 않는지 확인 | 4 | 통과 |
| `tests/test_docs_reference_integrity.py` | 사용자_매뉴얼.md가 인용하는 API 경로/심볼이 실제로 존재하는지 확인 | 16 | 통과 |
| `tests/test_docs_reference_integrity.py` | 설계서.md가 인용하는 클래스/함수명이 실제 소스에 그대로 남아있는지 확인 | 42 | 통과 |
| `tests/test_docs_reference_integrity.py` | 팀원용_접속가이드.md가 인용하는 심볼/설정이 실제로 존재하는지 확인 | 4 | 통과 |
| `tests/test_docs_reference_integrity.py` | 프로세스_명세서.md가 인용하는 심볼이 실제 소스에 그대로 남아있는지 확인 | 19 | 통과 |
| `tests/test_docs_reference_integrity.py` | 필수 문서 파일(매뉴얼/설계서/프로세스 명세서/접속가이드)이 실제로 존재하는지 확인 | 4 | 통과 |
| `tests/test_env_and_ui.py` | .env 값이 설정 API에 반영되고 CSV 다운로드 파일명이 올바른지 확인 | 1 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | - | 3 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | Discord 알림이 Slack과 다른 Discord 전용 스키마(content/embeds)로 전송되는지 확인 | 1 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | Jira 알림이 Basic 인증 헤더를 base64로 올바르게 인코딩하는지 확인 | 1 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | LLM 판정이 없을 때 pass_policy와 무관하게 룰 판정으로 대체되는지 확인 | 1 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | 검색품질 평가자가 Recall/Precision/MRR을 실제 값으로 정확히 계산하는지 확인 | 1 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | 룰 판정과 LLM 판정의 일치/불일치/평가없음 상태를 올바르게 계산하는지 확인 | 1 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | 설정 오버라이드가 커넥터/임계값에 올바르게 반영되는지 확인 | 1 | 통과 |
| `tests/test_evaluators_and_notifiers.py` | 실패율이 높은 카테고리에 대해 Jira 티켓이 생성되는지 확인 | 1 | 통과 |
| `tests/test_export_and_config_persistence.py` | 설정 저장 후 조회 및 리포트 내보내기 라우트가 정상 동작하는지 확인 | 1 | 통과 |
| `tests/test_external_monitor.py` | probe_once가 2xx 응답을 성공으로 기록하는지 확인 | 1 | 통과 |
| `tests/test_external_monitor.py` | probe_once가 네트워크 예외를 실패 결과로 안전하게 변환하는지 확인 | 1 | 통과 |
| `tests/test_external_monitor.py` | 너무 짧은 체크 주기를 입력해도 최소 주기(10초)로 강제 상향되는지 확인 | 1 | 통과 |
| `tests/test_external_monitor.py` | 외부 대상 레지스트리가 최근 체크 이력으로 가동률/평균응답시간을 올바르게 계산하는지 확인 | 1 | 통과 |
| `tests/test_external_monitor.py` | 외부 모니터링 대상 목록이 파일에 저장되어 재시작(재로딩) 후에도 유지되는지 확인 | 1 | 통과 |
| `tests/test_frontend_js_regression.py` | - | 9 | 통과 |
| `tests/test_independent_judge_kwargs.py` | - | 9 | 통과 |
| `tests/test_integrations.py` | Jira 연동이 꺼져 있을 때 알림을 조용히 건너뛰는지 확인 | 1 | 통과 |
| `tests/test_integrations.py` | Slack 웹훅이 설정되지 않았을 때 skipped 상태를 반환하는지 확인 | 1 | 통과 |
| `tests/test_integrations.py` | excel_io 로더가 JSON 데이터셋 파일도 문제없이 읽어오는지 확인 | 1 | 통과 |
| `tests/test_isolation_regression.py` | - | 9 | 통과 |
| `tests/test_jira_client.py` | - | 7 | 통과 |
| `tests/test_llm_client_providers.py` | Anthropic 응답이 마크다운 코드펜스로 감싸져 와도 JSON을 올바르게 벗겨내는지 확인 | 1 | 통과 |
| `tests/test_llm_client_providers.py` | Anthropic 제공자가 Messages API를 호출하고 응답 텍스트 블록을 올바르게 파싱하는지 확인 | 1 | 통과 |
| `tests/test_llm_client_providers.py` | Anthropic 제공자가 환경변수와 기본 모델/엔드포인트를 올바르게 읽어오는지 확인 | 1 | 통과 |
| `tests/test_llm_client_providers.py` | OpenAI가 기본 LLM 제공자이며 환경변수에서 키를 읽어오는지 확인 | 1 | 통과 |
| `tests/test_llm_client_providers.py` | 제공자를 '사용 안 함'으로 지정하면 키가 있어도 항상 비활성 상태인지 확인 | 1 | 통과 |
| `tests/test_llm_client_providers.py` | 커스텀 제공자가 Authorization이 아닌 사용자 지정 헤더명으로 키를 전송하는지 확인 | 1 | 통과 |
| `tests/test_llm_client_providers.py` | 커스텀 제공자는 키와 엔드포인트가 모두 있어야 활성화되는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | /metrics-addon이 k6 import 이후 k6 관련 지표도 포함하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | /metrics-addon이 자체 운영 지표를 Prometheus 텍스트 포맷으로 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | /monitoring-addon 페이지가 기존 메인 대시보드와 별도로 정상 서빙되는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | k6 결과 수동 import API 이후 최신/이력/상세 API가 일관되게 반영하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | k6 실행 이력 API가 데이터 없을 때 빈 목록을 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | k6 최신 결과 API가 데이터 없을 때 no_data를 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | 장기 스냅샷 조회 API가 저장된 스냅샷을 올바르게 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_api.py` | 존재하지 않는 실행 상세 조회 시 404를 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_db.py` | k6 실행 이력 조회가 페이지네이션과 Pass/Fail 필터를 올바르게 지원하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_db.py` | 같은 DB 파일에 대해 여러 번 스키마를 생성해도 에러 없이 멱등한지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_db.py` | 같은 run_id의 k6 실행을 두 번 저장해도 중복 저장(및 threshold 중복)이 안 되는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_db.py` | 스냅샷 저장 후 조회 시 최신순으로 정렬되는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_db.py` | 존재하지 않는 run_id 조회 시 None을 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_db.py` | 최신 k6 실행 조회가 가장 최근에 저장된 실행을 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_k6_import.py` | k6 결과 JSON에 필수 필드가 없을 때도 invalid_json으로 처리되는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_k6_import.py` | k6 결과 파일이 깨진 JSON일 때 invalid_json 상태를 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_k6_import.py` | k6 결과 파일이 없을 때 no_data 상태를 반환하는지 확인 | 1 | 통과 |
| `tests/test_monitoring_addon_k6_import.py` | 정상 k6 결과를 저장하고, 재수입 시 중복 저장하지 않는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | /health 엔드포인트가 HealthChecker 결과를 그대로 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | Anthropic 제공자가 Messages API를 올바른 헤더로 호출하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | Jira 티켓 목록 API가 배열 형태로 응답하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | LLM 제공자를 '사용 안 함'으로 설정하면 API 키가 있어도 LLM 평가를 건너뛰는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | get_local_ip()가 유효한 IPv4 형식 또는 None을 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 모니터링 애드온이 얹혀도 기존 /api/monitoring/summary 응답의 key 구조가 그대로인지 확인(회귀 방지) | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 모니터링 요약 API가 요청수/응답시간/헬스 상태를 실제로 집계해 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 문서 조회 API가 모든 문서 종류에 대해 파일명/본문을 올바르게 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 실행 결과 조회 및 실행 상세 조회 API가 올바른 run_id를 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 실행 상세 조회 API가 경로 조작(path traversal) 시도를 거부하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 외부 모니터링 대상 등록 시 http(s)로 시작하지 않는 URL은 거부되는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 외부 모니터링 대상 등록/목록조회/삭제 API가 정상 동작하고 최소 체크주기가 강제되는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 존재하지 않는 기법명을 요청하면 거부되는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 존재하지 않는 문서 키를 요청하면 404를 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 커넥터 기본값 API가 dataset_only 모드를 기본으로 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 커스텀 LLM 제공자가 사용자 지정 헤더/엔드포인트로 정확히 호출되는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 테스트 기법을 하나도 선택하지 않으면 실행 요청이 거부되는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 통과율이 임계값보다 낮으면 품질 게이트가 fail을 반환하는지 확인 | 1 | 통과 |
| `tests/test_new_endpoints_and_modules.py` | 파이프라인 벤치마크가 실제 실행 시간을 측정해 반환하는지 확인 | 1 | 통과 |
| `tests/test_pipeline.py` | - | 3 | 통과 |
| `tests/test_pipeline.py` | dataset_only 모드에서 저장된 답변이 없으면 오류로 처리되는지 확인 | 1 | 통과 |
| `tests/test_pipeline.py` | functional 기법이 커넥터 계약 검증용 합성 질문 4건을 실행하는지 확인 | 1 | 통과 |
| `tests/test_pipeline.py` | 검색된 문서가 없을 때 검색품질 평가가 실패로 처리되는지 확인 | 1 | 통과 |
| `tests/test_pipeline.py` | 단순 케이스 1건에 대해 파이프라인이 리포트를 정상적으로 생성하는지 확인 | 1 | 통과 |
| `tests/test_reporting_and_ui.py` | reporter가 JSON/CSV/마크다운 리포트 파일을 모두 생성하는지 확인 | 1 | 통과 |
| `tests/test_reporting_and_ui.py` | 루트 경로가 대시보드 HTML 페이지를 정상적으로 서빙하는지 확인 | 1 | 통과 |
| `tests/test_settings_and_exports.py` | 설정 저장 후 CSV/MD/JSON 형식의 리포트 다운로드가 모두 동작하는지 확인 | 1 | 통과 |
| `tests/test_ui_and_env_docs.py` | 설정 API가 기본값을 반환하는지 확인 | 1 | 통과 |
| `tests/test_ui_config_and_exports.py` | 엑셀 양식 다운로드와 실행/리포트 라우트가 정상 동작하는지 확인 | 1 | 통과 |
| `tests/test_users.py` | - | 13 | 통과 |
| `tests/test_voc_analysis.py` | - | 107 | 통과 |
| `tests/test_voc_analysis_api.py` | - | 89 | 통과 |

모든 테스트가 통과했습니다.

